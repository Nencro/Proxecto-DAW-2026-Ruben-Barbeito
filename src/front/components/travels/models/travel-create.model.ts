export interface TravelDestinationSuggestion {
  id: string;
  destino: string;
  pais: string;
  codigoPais: string;
  aeropuerto: string;
}

export interface CountrySuggestion {
  code: string;
  name: string;
  label: string;
}
